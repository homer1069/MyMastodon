import { StyleSheet } from 'react-native';

const red = "#A2273C";

export const globalStyles = StyleSheet.create({
    addMarginBottom: {
        marginBottom: 10
    },
    redColor: {
        color: red
    }
});
